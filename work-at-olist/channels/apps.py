"""Channels' application configuration."""
from django.apps import AppConfig


class ChannelsConfig(AppConfig):
    """ChannelsConfig holds Channels' application configurarion."""

    name = 'channels'
    verbose_name = 'Channels'
